name: protocol
description: Versioned protocol and immutable DTOs for Tinest.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  path: ^1.9.1

dev_dependencies:
  build_runner: ^2.16.0
  freezed: ^4.0.0
  json_serializable: ^6.14.1
  test: ^1.31.1
