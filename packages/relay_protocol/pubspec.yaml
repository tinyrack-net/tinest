name: relay_protocol
description: End-to-end encrypted relay wire contracts for Tinest.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  cryptography: ^2.9.0
  meta: ^1.19.0

dev_dependencies:
  test: ^1.31.1
